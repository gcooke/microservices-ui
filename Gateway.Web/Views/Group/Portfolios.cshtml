@using Gateway.Web.Database
@using Gateway.Web.Models
@using Gateway.Web.Models.Controller
@using Gateway.Web.Models.Group
@using Gateway.Web.Models.Shared
@using Gateway.Web.Utils
@using Newtonsoft.Json
@using Version = Gateway.Web.Models.Controller.Version
@{
    Layout = "~/Views/Shared/_Layout.NoFooter.cshtml";
    ViewBag.Title = @Model.Name;
    ViewBag.Current = "Portfolios";
}
@{ Html.RenderPartial("_GroupNav", (PortfoliosModel)@Model); }

<div id="page-content-wrapper">
    <div class="hero-unit">

        <br />
        <div id="containerx bulk">

            <table style="width: 100%; white-space: nowrap" class="datatable">
                <thead>
                    <tr>
                        <td></td>
                        <td>Level</td>
                        <td>Name</td>
                    </tr>
                </thead>
                @foreach (PortfolioModel item in Model.Items)
                {
                    <tr>
                        <td>
                            @Html.ActionLink(" ", "RemovePortfolio", "Group", new { id = item.Id }, new { @class = "text-center glyphicon glyphicon-trash" })
                        </td>
                        <td>@item.Level</td>
                        <td>@item.Name</td>
                    </tr>
                }
            </table>
            <br />

        </div>
        <br />

    </div>
</div>
