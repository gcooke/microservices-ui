@using Gateway.Web.Database
@using Gateway.Web.Models
@using Gateway.Web.Models.AddIn
@using Gateway.Web.Models.Controller
@using Gateway.Web.Models.Group
@using Gateway.Web.Utils
@using Newtonsoft.Json
@using Version = Gateway.Web.Models.Controller.Version
@model AddInsModel
@{
    Layout = "~/Views/Shared/_Layout.NoFooter.cshtml";
    ViewBag.Title = "Group";
    ViewBag.Current = "AddIns";
}
@{ Html.RenderPartial("_GroupNav", (AddInsModel)@Model); }

<div id="page-content-wrapper">
    <div class="hero-unit">

        <h3>Add-In Versions</h3>
        <br />


        <div id="containerx bulk">

            <table style="width: 100%; white-space: nowrap" class="datatable">
                <thead>
                    <tr>
                        <td>Name</td>
                        <td>Friendly Name</td>
                        <td>Version</td>
                        <td>Action</td>
                    </tr>
                </thead>
                @foreach (var item in Model.Items)
                {
                    <tr>
                        <td>@item.Name</td>
                        <td>@item.FriendlyName</td>
                        <td>@item.Version</td>
                        <td>
                            @Html.ActionLink("Remove", "RemoveAddInVersion", "Group", new { id = item.ExcelAddInVersionId, groupId = Model.Id }, new { @class = "hoverlink" })
                        </td>
                    </tr>
                }
            </table>
            <br />

        </div>
        <br />

        @using (Html.BeginForm("InsertAddInVersion", "Group", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
        {
            <a class="btn btn-primary" data-toggle="collapse" href="#collapsePanel" aria-expanded="false" aria-controls="collapsePanel">
                Add New Version
            </a>
            <div class="@(Html.ViewData.ModelState.IsValid ? "collapse" : "collapse show")" id="collapsePanel">
                <div class="info-block">
                    <div>
                        @Html.AntiForgeryToken()
                        @Html.ValidationSummary(false, "", new { @class = "text-danger" })

                        <!-- Hidden Id Field -->
                        @Html.TextBox("_id", Model.Id, new { @class = "form-control", @style = "display:none" })

                        <div class="container-fluid">
                            <div class="row">
                                <div class="col-md-2">
                                    <label class="control-label">Version</label>
                                </div>
                                <div class="col-md-4">
                                    @Html.DropDownList("_version", Model.AvailableVersions, new { @class = "form-control" })
                                </div>
                            </div>

                            <div class="col-md-offset-2 col-md-4">
                                <input type="submit" class="btn btn-primary" value="Add" />
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>

</div>
